Fix config schema compatibility and improve error handling

This commit applies 5 critical patches to fix config system bugs:

PATCHES APPLIED:
1. Fix DEFAULT_CONFIG - Add missing sections (model_config, forces, noise, rom)
2. Add OLD schema detection - Reject legacy configs with migration instructions
3. Use unified simulation backends - CLI now calls simulate_backend() consistently
4. Add imageio availability checks - Graceful degradation for video generation
5. Fix noise field naming - Change noise.type → noise.kind for consistency

CHANGES:
- src/rectsim/config.py: Complete DEFAULT_CONFIG, add schema validation
- src/rectsim/cli.py: Import unified backends, add imageio checks
- src/rectsim/unified_config.py: Fix noise.kind field name

TESTING:
- All 6 verification tests passing
- NEW schema configs load successfully (vicsek_morse_base.yaml)
- OLD schema configs rejected with helpful errors (gentle_clustering.yaml)
- Simulation backend tested and working (101 timesteps generated)
- Output format verified ROM-compatible

DOCUMENTATION:
- CODEBASE_AUDIT_REPORT.md: 28 bugs documented
- CONFIG_SCHEMA_PATCHES.md: 8 specific fixes
- MIGRATION_GUIDE.md: Complete OLD→NEW schema guide
- TEST_PATCHES.md: Verification results
- PATCH_STATUS.txt: Executive status report

CONFIG STATUS:
- 8 configs using NEW schema (ready to use)
- 14 configs using OLD schema (need migration)
- Migration guide and script provided

IMPACT:
- Prevents KeyError crashes on missing config keys
- Clear error messages for schema mismatches
- Consistent simulation interface across codebase
- Graceful handling of missing dependencies

See PATCH_STATUS.txt for complete details.
